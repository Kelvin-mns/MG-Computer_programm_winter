import math
num = int(raw_input("Enter a number to be squared: "))
def square(num):
    y = math. pow(num,2)
    return y
print str(num) + "squared is equal to " + str(square(num))

return ^2
#b
 import mathded caculate_average();

